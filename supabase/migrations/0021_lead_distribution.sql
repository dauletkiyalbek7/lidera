-- Раздача лидов по кругу (ТЗ, Блок 2: Лиды, Кабинет менеджера).
--
-- «На смене» — кто сейчас работает и получает входящие лиды. Раздача идёт
-- только этим людям; ночью, когда никого нет, лид висит «новым», а утром РОП
-- раздаёт накопленное одной кнопкой. Позже признак будет ставить раздел
-- «Посещения» по GPS-геозоне офиса.
alter table project_members add column on_shift boolean not null default false;

-- Цена пробного урока — настройка проекта (по умолчанию 990 ₸).
alter table projects add column trial_price numeric not null default 990
  check (trial_price >= 0);
